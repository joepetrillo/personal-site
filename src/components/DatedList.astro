---
interface Props {
  ariaLabel?: string;
}

const { ariaLabel } = Astro.props;
---

<ol class="dated-list" aria-label={ariaLabel}>
  <slot />
</ol>

<style>
  .dated-list {
    display: grid;
    gap: var(--space-list);
  }
</style>
